# 1. 가상환경 폴더 (절대 올리면 안 됨!)
.venv/
venv/
env/

# 2. 보안 파일 (API 키 숨김!)
.env

# 3. 파이썬 캐시 파일 (쓸데없는 용량 차지)
__pycache__/
*.pyc

# 4. 벡터 DB 로컬 파일 (프론트엔드 개발자가 새로 생성하도록 유도)
qdrant_db_local/

# 5. 맥/윈도우 OS 시스템 파일
.DS_Store
Thumbs.db

# 6. 프론트엔드 패키지 (npm install로 재생성)
node_modules/
frontend/node_modules/